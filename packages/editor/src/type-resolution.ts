/** Compatibility exports; semantic type resolution is owned by core. */
export {
  resolveTypes,
  materializeDocument,
  invalidateTypeResolution,
  resolutionKey,
  type ResolvedNode,
  type TypeResolution,
} from 'mtlx-core/session';
